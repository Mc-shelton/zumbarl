import { useEffect, useState } from 'react'
import { FiArrowLeft, FiArrowRight, FiPlus, FiTrash2, FiX } from 'react-icons/fi'
import { uploadZumbarlFile } from '../../../lib/uploadZumbarlFile'
import { normalizeZumbarlFileUrl } from '../../../lib/normalizeZumbarlFileUrl'

function ExplorePostEditModal({ post, onClose, onSave }) {
  const [body, setBody] = useState(''); const [items, setItems] = useState([]); const [activeIndex, setActiveIndex] = useState(0); const [isSaving, setIsSaving] = useState(false); const [error, setError] = useState('')
  useEffect(() => { if (!post) return; setBody(post.copy || ''); setItems((post.gallery || []).map((url, index) => ({ url, previewUrl: url, edit: post.mediaEdits?.[index] || { type: post.tag === 'Video' ? 'video' : 'image', zoom: 1, positionX: 50, positionY: 50 } }))); setActiveIndex(0); setError('') }, [post])
  if (!post) return null
  const active = items[activeIndex]
  const addFiles = (files) => { const additions = [...files].slice(0, 8 - items.length).map((file) => ({ file, previewUrl: URL.createObjectURL(file), edit: { type: file.type.startsWith('video/') ? 'video' : 'image', zoom: 1, positionX: 50, positionY: 50, trimStart: 0 } })); setItems((current) => [...current, ...additions]); if (additions.length) setActiveIndex(items.length) }
  const patchActive = (patch) => setItems((current) => current.map((item, index) => index === activeIndex ? { ...item, edit: { ...item.edit, ...patch } } : item))
  const remove = (index) => { setItems((current) => current.filter((_, i) => i !== index)); setActiveIndex((current) => Math.max(0, Math.min(current > index ? current - 1 : current, items.length - 2))) }
  const move = (index, direction) => { const target = index + direction; if (target < 0 || target >= items.length) return; setItems((current) => { const next = [...current]; [next[index], next[target]] = [next[target], next[index]]; return next }); setActiveIndex(target) }
  async function submit(event) { event.preventDefault(); if (!body.trim() || isSaving) return; setIsSaving(true); setError(''); try { const mediaUrls = await Promise.all(items.map(async (item) => { if (!item.file) return item.url; const upload = await uploadZumbarlFile(item.file, { scope: 'connect-post', metadata: { purpose: 'post-edit' } }); return normalizeZumbarlFileUrl(upload.url || upload.previewUrl) })); await onSave(post.id, { body: body.trim(), mediaUrls, mediaEdits: items.map((item) => item.edit) }); onClose() } catch (requestError) { setError(requestError.message || 'Could not update your post.') } finally { setIsSaving(false) } }
  return <div className="explore-post-edit-backdrop" onMouseDown={(event) => { if (event.target === event.currentTarget && !isSaving) onClose() }}><form className="explore-post-edit-modal is-comprehensive" onSubmit={submit}><header><div><span>Edit post</span><h2>Update your post</h2></div><button type="button" onClick={onClose}><FiX /></button></header><div className="explore-post-edit-body"><textarea value={body} onChange={(event) => setBody(event.target.value)} autoFocus />{items.length ? <><div className="explore-post-media-stage">{active?.edit.type === 'video' ? <video src={active.previewUrl} controls /> : <img src={active?.previewUrl} alt="Post media preview" style={{ objectPosition: `${active?.edit.positionX ?? 50}% ${active?.edit.positionY ?? 50}%`, transform: `scale(${active?.edit.zoom || 1})` }} />}</div><div className="explore-post-media-thumbs">{items.map((item, index) => <article key={`${item.previewUrl}-${index}`} className={index === activeIndex ? 'is-active' : ''}><button type="button" className="explore-post-thumb-preview" onClick={() => setActiveIndex(index)}>{item.edit.type === 'video' ? <video src={item.previewUrl} /> : <img src={item.previewUrl} alt="" />}<span>{index + 1}</span></button><div><button type="button" disabled={!index} onClick={() => move(index, -1)}><FiArrowLeft /></button><button type="button" onClick={() => remove(index)}><FiTrash2 /></button><button type="button" disabled={index === items.length - 1} onClick={() => move(index, 1)}><FiArrowRight /></button></div></article>)}</div>{active?.edit.type === 'image' ? <div className="explore-post-edit-controls"><label>Zoom<input type="range" min="1" max="3" step=".05" value={active.edit.zoom || 1} onChange={(event) => patchActive({ zoom: Number(event.target.value) })} /></label><label>Horizontal<input type="range" min="0" max="100" value={active.edit.positionX ?? 50} onChange={(event) => patchActive({ positionX: Number(event.target.value) })} /></label><label>Vertical<input type="range" min="0" max="100" value={active.edit.positionY ?? 50} onChange={(event) => patchActive({ positionY: Number(event.target.value) })} /></label></div> : <div className="explore-post-edit-controls"><label>Start<input type="number" min="0" step=".1" value={active?.edit.trimStart || 0} onChange={(event) => patchActive({ trimStart: Number(event.target.value) })} /></label><label>End<input type="number" min=".1" step=".1" value={active?.edit.trimEnd || ''} onChange={(event) => patchActive({ trimEnd: event.target.value ? Number(event.target.value) : undefined })} /></label></div>}</> : null}{items.length < 8 ? <label className="explore-post-edit-add"><FiPlus /> Add more media<input type="file" multiple accept="image/*,video/*" onChange={(event) => { addFiles(event.target.files); event.target.value = '' }} /></label> : null}{error ? <p role="alert">{error}</p> : null}</div><footer><button type="button" onClick={onClose}>Cancel</button><button type="submit" disabled={!body.trim() || isSaving}>{isSaving ? 'Saving…' : 'Save changes'}</button></footer></form></div>
}
export default ExplorePostEditModal
